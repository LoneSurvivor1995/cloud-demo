server:
  port: 8771
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LoneSurvivor1995/spring-cloud-config-file
          search-paths: repo
          username:
          password:
          label: master